function solve(type, _p , _h){
                        // type - leap (високосна година) или normal (невисокосна).
    let p = Number(_p); //брой празници в годината (които не са събота и неделя).
    let h = Number(_h); //брой уикенди, в които Влади си пътува до родния град.

    

    
}

solve("leap", "5", "2");