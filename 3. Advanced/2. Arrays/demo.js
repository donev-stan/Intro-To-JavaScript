// Arrays are reference type (address pointing to a specific place in the memory where the array is stored)

// Ctrl + Shift + Space = popup help menu 

// ...params - rest operator

// .fill() - doesnt change the lenght, it just fills
let array = [5, 6, 8];
array.fill(0, 1, 3);
console.log(array);