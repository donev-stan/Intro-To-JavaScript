function addNums(a, b){
    return a + b;
}

function compareNums(a, b){
    return b - a;
}

/*
let actions = {
    addNums,
    compareNums
};
module.exports = actions;
*/

module.exports = {
    addNums,
    compareNums
};